import React from "react";
import { Settings } from "../components/layouts/Settings";

const settings = () => {
  return (
    <div className="flex items-center justify-between px-5 h-screen bg-gray-500">
      <Settings />
    </div>
  );
};

export default settings;
